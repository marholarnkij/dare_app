<% if signed_in? %>
  <div class="row">
    <aside class="span4">
      <section>
        <%= render 'shared/user_info' %>
      </section>
      <section>
        <%= render 'shared/micropost_form' %>
      </section>
    </aside>
    <div class="span8">
    	<h3>Micropost Feed</h3>
    	<%= render 'shared/feed' %>
    </div>
  </div>  
<% else %>
	<div class="center hero-unit">
  	<h1>โครงการ D.A.R.E ประเทศไทย</h1>

  	<h2>
    	This is the home page for the
    	<a href="#">โครงการ D.A.R.E ประเทศไทย</a>
    	sample application.
  	</h2>

  	<%= link_to "สมัครเข้าใช้", signup_path , class: "btn btn-large btn-primary" %>
	</div>

	<%= link_to image_tag("darelogo.gif", alt: "d.a.r.e.logo"), root_path %>
<% end %>